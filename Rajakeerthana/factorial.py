num=int(input("Enter the number"))
fact=1
for i in range(1,num+1):
    fact=fact*i
print(fact)
# Pseudocode

# User Input
# Initialising the variable(fact) which starts from 1
# For loop to iterate the numbers from 1 to num+1
# Multiplying fact into i,so that fact value get changed for each iteration
# Printing final output